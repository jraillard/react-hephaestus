{{#if useHooks}}import { TFunction } from {{quotes}}react-i18next{{quotes}};{{/if}}

{{#if useHooks}}
export type UseDataProps = {
	/**
	 * Translator
	 */
	t: TFunction;
};
{{/if}}
export type {{componentName}}Props = {
	/**
	 * Cypress selector specifier from parent
	 */
	dataCy: string;
};